name: test-build-msvc
on: [push]
jobs:
  test-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ilammy/msvc-dev-cmd@v1
        with:
          vsversion: "2022"
      - run: |
          mkdir build
          cd build
          cmake ..
          
